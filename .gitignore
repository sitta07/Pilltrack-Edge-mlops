# ==========================================
# 🐍 Python Standard Ignores
# ==========================================
__pycache__/
*.py[codz]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ==========================================
# 📦 Environment & Virtual Envs
# ==========================================
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# ==========================================
# 🛠 Tools & IDEs
# ==========================================
# VS Code
.vscode/
*.code-workspace

# PyCharm / JetBrains
.idea/

# Mac OS
.DS_Store

# Jupyter Notebooks
.ipynb_checkpoints
profile_default/
ipython_config.py

# ==========================================
# 📊 Logs & Testing
# ==========================================
*.log
pip-log.txt
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ==========================================
# 🤖 MLOps & DVC (The Important Part) 🌟
# ==========================================

# 1. DVC Internal Cache (Ignore these)
.dvc/cache
.dvc/tmp
.dvc/plots

# 2. MLflow Local Logs (Ignore if using remote tracking, or keep if local)
# mlruns/ 
# (ถ้าอยากเก็บ log ไว้ดูในเครื่องให้ใส่ # ข้างหน้า mlruns/ แต่ถ้า log เยอะให้เอา # ออก)

# 3. Data & Models (Ignore actual large files)
/data/*
/models/*

# 4. 🚨 EXCEPTIONS (IMPORTANT!) 🚨
# บอก Git ว่า "ห้าม Ignore ไฟล์พวกนี้" ถึงแม้จะอยู่ในโฟลเดอร์ที่ Ignore ก็ตาม
!/data/*.dvc
!/models/*.dvc
!/data/.gitignore
!/models/.gitignore

# Keep specific small config files if needed
!models/labels.json
!models/model_metadata.json